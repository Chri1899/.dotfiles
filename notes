fonts:
	Font-Awesome: sudo apt install fonts-font-awesome
	InconsolataGo Nerd Font: 1. Download via wget =  curl -OL "https://github.com/ryanoasis/nerd-fonts/releases/latest/download/InconsolataGo.tar.xz"
				 2. If there is no font dir, make a font dir at $HOME/.local/share/fonts
				 3. Copy tar to font-dir: mv InconsolataGo.tar.xz $HOME/.local/share/fonts
				 4. Extract the tarball: sudo tar -xf InconsolataGo.tar.xz
				 5. Remove tar file = rm InconsolataGo.tar.xz

Installs:
	1. Update System
		-sudo apt update, sudo apt upgrade
	2. Install prerequisited:
		- sudo apt install git stow tar gcc make bzip2 curl
	3. Install Guest Additions if on VM:
		- sudo apt install linux-headers-generic sudo apt install linux-headers-$(uname -r)
		- Insert Guest Additions
		- Copy files to Documents
		- Move to Documents
		- sudo chmod 777 VBoxLinuxAdditions.run
		- sudo ./VBoxLinuxAdditions.run
	4. Configure GIT
		- git config --global user.name "chri1899"
		- git config --global user.email "c.b-1@outlook.de"
	5. Install all wanted fonts
		- bash fonts.sh
	5. Install window manager, terminal emulator and shell
		- sudo apt install i3
		- sudo apt install alacritty
		- sudo apt install zsh
			a. Change default shell to zsh: chsh -$(which zsh)
	6. Install third party programs
		- NVIM:
			a. sudo apt install neovim
		- IDEA:
			a.
		- BRAVE:
			a.
	7. Install Programming Language packages and tools
		- Python:
			a. sudo apt install python3
		- Java:
			a. sudo apt-get install openjdk-17-jdk || JRE 17
			b. sudo apt install openjdk-21-jdk
		- Gradle:
			a. sudo apt install gradle
		- FZF
			a. sudo apt install fzf

Dotfiles Structure:
	
	.dotfiles/
		- scripts/
		- .config/
			- i3/
			- alacritty/
		.zshrc
		.zsh_aliases
		.p10k.zsh
		.stow-local-ignore
